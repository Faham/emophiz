using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SensorLib.Sensors.Inputs
{
    /*
    class RampInput : IInput<float>
    {
        public RampInput() : this(0) {}

        public RampInput(float slope_)
        {
            slope = slope_;
            count = 0.0f;
            return;
        }

        public void Reset()
        {
            count = 0.0f;
        }

        public float[] GetData(int maxSamples)
        {
            float[] data = new float[maxSamples];
            for (int i = 0; i < data.Length; i++)
            {
                data[i] = count;
                count += slope;
            }
            return data;
        }

        readonly float slope;
        float count;
    }
     * */
}
